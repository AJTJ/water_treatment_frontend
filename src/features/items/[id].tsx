import React from "react";
import { ItemDetails } from "common/components";

export const ItemPage: React.FC = () => {
  // const { id } = useParams<{ id: string }>();

  // TODO: Get Item details by ID

  return (
    <div>
      <ItemDetails />
    </div>
  );
};

export default ItemPage;
